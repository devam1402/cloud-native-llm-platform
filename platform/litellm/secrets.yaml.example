# Copy to secrets.yaml, fill in a real master key, apply MANUALLY (gitignored):
#   kubectl apply -f secrets.yaml
# Production answer: External Secrets Operator / Sealed Secrets — see DECISIONS.md
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: litellm
stringData:
  master-key: "sk-CHANGE-ME-platform-2026"
